NEXT_PUBLIC_SUPABASE_URL=https://zjlseqcyeewoksbquung.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ER0fE5qDlb6ww4VsiibsHA_hTkiltj8

# Server-only secret — get a free key at https://console.groq.com/keys
# Never commit a real value here; this file is checked into git.
GROQ_API_KEY=

# Web Push (browser notifications) — generate your own free keypair with:
#   npx web-push generate-vapid-keys
# Never commit real values here; this file is checked into git.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Shared secret checked against the Authorization header Vercel Cron sends —
# generate any random string, e.g. `openssl rand -hex 24`.
CRON_SECRET=

# Server-only — from Supabase dashboard: Project Settings -> API -> service_role secret.
# Bypasses RLS; used only by the notifications cron route to read across all owners.
# Never commit a real value here; this file is checked into git.
SUPABASE_SERVICE_ROLE_KEY=
